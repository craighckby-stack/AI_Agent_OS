# AI_Agent_OS
Local agent kernel — routes requests through a module loader with flat-file memory and multi-provider LLM fallback (Gemini/OpenAI/DeepSeek/local). Runs unchanged in Colab or on-device via Termux. No cloud dependency required.
